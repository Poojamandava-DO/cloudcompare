apiVersion: v1
kind: Service
metadata:
  name: cloudcompare-service
  namespace: cloudcompare
spec:
  type: LoadBalancer
  selector:
    app: cloudcompare
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
